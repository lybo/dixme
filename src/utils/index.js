import * as generic from './generic';
import * as environment from './environment';
import * as textSelection from './textSelection';

export default Object.assign({},
  generic,
  environment,
  textSelection,
);

